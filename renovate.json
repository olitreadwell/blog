{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "packageRules": [
    {
      "description": "TypeScript majors are held back: @astrojs/check allows ^5 || ^6 and typescript-eslint allows below 6.1, so version 7 breaks npm ci with ERESOLVE.",
      "matchPackageNames": ["typescript"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    }
  ]
}
